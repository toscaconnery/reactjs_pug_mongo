extends layout

block content
    h1= article.title
    h5 Writen by #{article.author}
    p= article.body
    hr
    a.btn.btn-warning(href='/article/edit/' + article._id) Edit
    a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete
